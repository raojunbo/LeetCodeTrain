/*
 * @lc app=leetcode.cn id=84 lang=swift
 *
 * [84] 柱状图中最大的矩形
 */

// @lc code=start
class Solution84 { 
//    最大矩形：
//    因为矩形是由x于y的值得乘积；可以先让x之间的距离最大；
//    找小值向里缩进；
    // 保存最
    func largestRectangleArea(_ heights: [Int]) -> Int {
        let resutl = 0
        let left = 0
        let right = heights.count - 1
        while left < right {
            
        }
        return 1
    }
}
// @lc code=end

